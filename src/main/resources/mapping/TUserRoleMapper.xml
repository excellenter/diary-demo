<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="personal.mapper.TUserRoleMapper">
    <resultMap id="BaseResultMap" type="personal.entity.TUserRole">
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="role_id" jdbcType="BIGINT" property="roleId"/>
    </resultMap>
    <insert id="insert" parameterType="personal.entity.TUserRole">
        insert into  (user_id, role_id)
        select #{userId,jdbcType=VARCHAR}, #{groupId,jdbcType=BIGINT} from dual where (select count(*) from base_user_role
        where user_id=#{userId,jdbcType=VARCHAR} and group_id=#{groupId,jdbcType=BIGINT})=0
    </insert>

    <select id="getRoleByUser" resultType="java.util.Map">
        select ur.role_id,r.name from base_user_role ur, base_role r where ur.role_id=r.id and ur.user_id=#{id, jdbcType=VARCHAR}
    </select>

    <delete id="deleteByUserAndRole">
        delete from base_user_role where user_id = #{user_id, jdbcType=VARCHAR} and role_id = #{role_id, jdbcType=BIGINT}
    </delete>

    <delete id="deleteByUser">
        delete from base_user_role where user_id = #{user_id, jdbcType=VARCHAR}
    </delete>
</mapper>