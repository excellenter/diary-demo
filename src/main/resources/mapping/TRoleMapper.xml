<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="personal.mapper.TRoleMapper">
    <resultMap id="BaseResultMap" type="personal.entity.TRole">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, name, remark
    </sql>
    <insert id="insert" parameterType="personal.entity.TRole">
        insert into base_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            id,
            name,
            <if test="remark != null">
                remark,
            </if>
        </trim>
        <trim prefix="select " suffix=" from dual" suffixOverrides=",">
            #{id,jdbcType=VARCHAR},
            #{name,jdbcType=VARCHAR},
            <if test="remark != null">
                #{remark,jdbcType=VARCHAR},
            </if>
        </trim>
        <!-- id, name 不允许重复 -->
        where (select count(*) from base_role where id = #{id,jdbcType=VARCHAR} or name = #{name,jdbcType=VARCHAR})=0
    </insert>

    <delete id="deleteBatchInId">
        delete from base_role where id in (
        <foreach collection="ids" item="id" index="index" separator=",">
            #{id}
        </foreach>)
    </delete>
    <delete id="deleteInId">
        delete fron base_role where id = #{id}
    </delete>

    <update id="update" parameterType="personal.entity.TRole">
        update base_role
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR}
            </if>
            <if test="remark != null">
                remark = #{remark,jdbcType=VARCHAR}
            </if>
        </set>
        where id = #{id,jdbcType=VARCHAR}
    </update>

    <select id="getInId" resultType="personal.entity.TRole">
        select
        <include refid="Base_Column_List"/>
        from base_role where id = #{id}
    </select>

    <select id="listInIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from where id in (
        <foreach collection="ids" item="id" index="index" separator=",">
            #{id}
        </foreach>)
    </select>

    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from base_role
    </select>
</mapper>